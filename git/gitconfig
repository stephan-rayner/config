[user]
  name = Stephan Rayner
  email = srayner@kabaminc.com

[core]
  editor = vim
  whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
  #excludesfile = ~/.gitignore

[status]
  submoduleSummary = true

[color]
  ui = auto
[color "branch"]
  current = yellow bold
  local = green bold
  remote = cyan bold
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold
  whitespace = red reverse
[color "status"]
  added = green bold
  changed = yellow bold
  untracked = red bold

[diff]
  tool = vimdiff
[difftool]
  prompt = true
[github]
  user = stephan-rayner
  token = token
[gitflow "prefix"]
  feature = feature-
  fix = fix-
  release = release-
  hotfix = hotfix-
  support = support-
  research = research-
  versiontag = v

[alias]
  l = log --oneline
  lg = log --oneline --graph --decorate
